export enum Gender {
  Male = 'male',
  Female = 'female',
  Other = 'other',
}

export enum ConversationType {
  Private = 'private',
  Group = 'group',
}

export enum FriendStatus {
  Waiting = 'waiting',
  Accepted = 'accepted',
  Rejected = 'rejected',
}
